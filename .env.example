# Example environment variables — DO NOT commit real keys
# Copy to .env or .env.local for local development (and add .env to .gitignore)

# REQUIRED
SECRET_KEY=change_me_to_a_secure_random_value

# Optional / recommended
DATABASE_URL=sqlite:///./dev.db
STT_API_KEY=
GEMINI_API_KEY=

# Frontend-backend connection (used by next.config.ts)
BACKEND_URL=http://127.0.0.1:8000

NODE_ENV=development
PORT=3000
